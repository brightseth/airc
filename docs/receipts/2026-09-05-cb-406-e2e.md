# CB-004/009/010/011 + multi-round — end-to-end receipt, production, 2026-09-05T21:47Z

Sender northstar_b → recipient northstar_a (dedicated lab principals via conformance/lib/live-sender.js; no human credential).
Approver in the recovery path: SCRIPTED (approves every preview; there is no human here). Contract: composition-boundary 0.1.2 (vibe-platform#406, PR #407).
Runner: conformance/cross-runtime/cb-406.test.js — positive (pre-normalized digest), raw digest → 409, recovery with fresh approval per round.

```
✓ CB-004 normalized digest → stored first time — HTTP 200 stored="hi"
✓ CB-004 raw digest → 409 server_normalized + server_text + server_sha256, nothing stored — HTTP 409 reason=server_normalized server_text="hi"
✓ CB-004 recovery: 1 round(s), each a fresh (scripted) approval → stored == last preview — outcome=sent rounds=1 previews=["hi"] stored="hi"
✓ CB-009 normalized digest → stored first time — HTTP 200 stored="hello"
✓ CB-009 raw digest → 409 server_normalized + server_text + server_sha256, nothing stored — HTTP 409 reason=server_normalized server_text="hello"
✓ CB-009 recovery: 1 round(s), each a fresh (scripted) approval → stored == last preview — outcome=sent rounds=1 previews=["hello"] stored="hello"
✓ CB-010 normalized digest → stored first time — HTTP 200 stored="hi"
✓ CB-010 raw digest → 409 server_normalized + server_text + server_sha256, nothing stored — HTTP 409 reason=server_normalized server_text="hi"
✓ CB-010 recovery: 1 round(s), each a fresh (scripted) approval → stored == last preview — outcome=sent rounds=1 previews=["hi"] stored="hi"
✓ CB-011 normalized digest → stored first time — HTTP 200 stored="hithere"
✓ CB-011 raw digest → 409 server_normalized + server_text + server_sha256, nothing stored — HTTP 409 reason=server_normalized server_text="hithere"
✓ CB-011 recovery: 1 round(s), each a fresh (scripted) approval → stored == last preview — outcome=sent rounds=1 previews=["hithere"] stored="hithere"
✓ multi-round pre-normalized digest of a non-idempotent input → 409 with the NEXT preview (rule not idempotent, as published) — HTTP 409 server_text="&"
✓ multi-round raw digest → 409 server_normalized + server_text + server_sha256, nothing stored — HTTP 409 reason=server_normalized server_text="&amp;"
✓ multi-round recovery: 2 round(s), each a fresh (scripted) approval → stored == last preview — outcome=sent rounds=2 previews=["&amp;","&"] stored="&"

cb-406: 15/15 pass
```
